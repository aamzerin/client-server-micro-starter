spring:
  application:
    name: client-micro-starter-service

  profiles:
    active:
    - localzone
    
  cloud:
    config:
      uri: http://localhost:8889
  main:
    web-application-type: reactive
    
server:
  port: 0